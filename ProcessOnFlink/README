sale_random_data  两个主题
person_random_data

1.将sale数据和person数据合并

窗口有三种，滚动窗口，滑动窗口，会话窗口。


TumblingEventTimeWindows 滚动窗口,就是跑不过,猜测和时区有关。
会话窗口：窗口的开始和结束是不固定的，根据数据的时间间隔来确定。

forMonotonousTimestamps 有序数据
forBoundedOutOfOrderness 乱序数据
forMonotonousTimestamps是forBoundedOutOfOrderness的特例，是outOfOrdernessMillis为0的特例。

2.流批join
这个术语应该是维表join
大致思路有两种：
1.将维表数据写入到状态中，然后在每个task中进行join
2.将维表数据广播到每个task中，然后在每个task中进行join

但是具体实现的时候还会根据其他细节有不同的具体实现方式。在不使用sql和table接口的情况下分别进行测试
https://blog.csdn.net/m0_63475429/article/details/127417649
https://blog.csdn.net/mm_ren/article/details/117095348


3.Sink


